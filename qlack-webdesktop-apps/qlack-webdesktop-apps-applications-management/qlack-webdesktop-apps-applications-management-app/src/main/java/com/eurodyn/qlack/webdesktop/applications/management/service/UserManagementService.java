package com.eurodyn.qlack.webdesktop.applications.management.service;

import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Service;

@Service
@RequiredArgsConstructor
public class UserManagementService {

//  @Autowired
//  private final UserService userService;
//
//  public Page<UserDTO> findAllUsers(Predicate predicate, Pageable pageable) {
//    userService.findAll(predicate, pageable);
//    return null;
//  }

//  private void updateUser(UserDTO userDTO) {
//    User user = ReturnOptional.r(repository.findById(userDTO.getId()));
//
//    userDTO.setEmail(user.getEmail());
//    if (StringUtils.isEmpty(userDTO.getPassword())) {
//      userDTO.setPassword(user.getPassword());
//    }
//
//    mapper.map(userDTO, user);
//  }
}
